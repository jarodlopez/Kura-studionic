{
  "name": "kodia-shop-template",
  "version": "1.0.0",
  "description": "Kodia Shop — plantilla white-label de tienda online (React + Firebase).",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o dist/styles.css --minify",
    "build:js": "node build.js",
    "build": "npm run build:css && npm run build:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.19"
  }
}
