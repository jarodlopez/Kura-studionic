{
  "name": "kura-studionic",
  "version": "1.0.0",
  "description": "KURA STUDIO — tienda streetwear (React + Firebase, sin framework de build pesado).",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o dist/styles.css --minify",
    "build:js": "node build.js",
    "build": "npm run build:css && npm run build:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.19"
  }
}
