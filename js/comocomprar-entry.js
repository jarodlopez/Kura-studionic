// Página /como-comprar — guía completa del proceso de compra y pago.
// Globals: React, ReactDOM. Componente: window.PaymentGuide (variant="page").

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<PaymentGuide variant="page" />);
